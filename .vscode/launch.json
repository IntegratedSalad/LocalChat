{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch LocalChat (LLDB)",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/LocalChat",
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {},
            "stopOnEntry": false,
            "initCommands": [
                "settings set target.process.thread.step-avoid-regexp ''"
            ]
        },
        {
            "name": "Attach to LocalChat",
            "type": "lldb",
            "request": "attach",
            "pid": "${command:pickProcess}",
            "initCommands": [
                "settings set target.process.thread.step-avoid-regexp ''"
            ]
        }
    ]
}